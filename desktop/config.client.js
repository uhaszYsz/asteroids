// Desktop (Neutralino) client server target.
// Leave empty to auto-discover (tries localhost:8765 after the shell origin).
// For a public Steam / release build, set your dedicated game server:
//   window.ASTEROIDS_SERVER = 'https://your-game-server.example.com';
window.ASTEROIDS_SERVER = 'http://127.0.0.1:8765';
